from setuptools import setup

setup(name='sURLi',
      version='0.1',
      description='A simple way to retrieve contents of a URL',
      url='',
      author='Evan Gaustad',
      author_email='evan.gaustad@gmail.com',
      packages=['sURLi'],
      install_requires=['selenium', 'Pillow'],
      zip_safe=True)
